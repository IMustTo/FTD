# Pull base image
FROM nodejs

MAINTAINER chen xin

# Install node packages global
# forever: 进程管理
RUN npm i -g forever

# nodemon: 自启动
RUN npm i -g nodemon

# vue-cli: vuejs模板脚手架
RUN npm i -g vue-cli
